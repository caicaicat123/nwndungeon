# nwndungeon — 随机副本插件（项目说明）

> 本文件是本项目的**更新入口**：项目后续的更新都记到这里（约定见 `..\用户手册.md`）。
> 原项目根目录的 `未来发展方向.md`（产品方向）与 `交接.md` §6（插件与实测）已并入本文附录 A / B，原文件已删除。
> 最后更新：2026-09-18

## 速查（2026-09-18）

- 版本：**1.3.0**（波次刷怪 / 读条进本 / 通关结算 GUI / S-A-B 评级 = 方向文档「第一步」四件套）；2026-09-18 深夜 RCON 实测通过。
- 权限节点：`nwndungeon.use`（默认 true：进本、`/dungeon leave`）、`nwndungeon.admin`（默认 op：`spawn/test/list/tp/reload`）。
- 编译：`nwndungeon\build.ps1`（需要 `simpfun-ops\opsbridge\lib\paper-api.jar` + `C:\Program Files\Java\jdk-21`）；产物版本号从 `plugin.yml` 读。
- **配置双份**：仓库 `config.yml` 与服务器 `plugins/NWNDungeon/config.yml` 要同时改（2026-09-18 才发现服务器那份一直是旧原型格式，已备份为 `.bak-20260918` 并上传与仓库一致的版本）。
- 发布：MineBBS 资源已提交（`NWNDungeon - 随机副本入口插件` v1.3.0，状态=审核中）；**GitHub 仓库已建并推送**
  https://github.com/caicaicat123/nwndungeon （公开，默认分支 main）。
  推送方式：`node simpfun-ops\tools\push-nwndungeon.cjs [提交信息]`（走 GitHub REST API，因为本机 git 没有 remote-https；仓库不存在会自动创建）。
  仓库内容：`src/`、`README.md`、`CHANGELOG.md`、`config.yml`、`plugin.yml`、`build.ps1`、`.gitignore`、`dist/`（1.1.0~1.3.0 成品 jar）、`说明.md`（本文件）。
  **MIT License**（2026-09-18 加 `LICENSE`，Copyright (c) 2026 New World Network；README 已同步说明）。
  Release：**v1.3.0 已发布**，jar 作为资产挂在 https://github.com/caicaicat123/nwndungeon/releases/tag/v1.3.0
  （发布脚本：`node simpfun-ops\tools\release-nwndungeon.cjs [v1.3.0]`，建 release 并把 `dist/` 里对应版本的 jar 传成资产）。
- 重启注意：换 jar 只能靠重启（流程见 `..\simpfun-ops\说明.md`）；要重启的改动攒一批一起上。
- 同类坑：`/kill` 被 Essentials 抢注（脚本/命令里必须写 `minecraft:kill`）；`/pm` 被 PvPManager 抢注（全球市场改用 `/playermarket open`）；CrazyCrates 的 `/crate` 已改 `/crates`。

## 副本实测方法（可复用，2026-09-18 核实）

`dungeon test iron` 建测试本 → `execute in minecraft:nwndungeon run forceload add <范围>`（**验方块前必须强制加载区块**）→ `execute in minecraft:nwndungeon if block <x y z> <方块[状态]>` 验方块 → `execute in minecraft:nwndungeon positioned <x y z> run minecraft:kill @e[type=...,distance=..N]` 清怪 → `data get block <箱子> Items` 验箱子内容。完事记得 `forceload remove all`。

实测通过项：门 `facing=east` 真挡住、清完一间才出下一间按钮、补给箱/最终奖励箱都有物品、中央木门正常出现、清完全部房间槽位自动释放。

## 附录 A：原 `未来发展方向.md` — 产品方向（2026-09-12）

# 未来发展方向

> 记录这个项目的产品方向与待决策项。
> 配合 `交接.md`（项目现状与运维通道）和 `USER_MANUAL.md`（协作规则）一起看。
> 最后更新：2026-09-12
> （2026-09-18 整理备注：`交接.md` 已并入 `..\simpfun-ops\说明.md` 与本文附录 B；`USER_MANUAL.md` 即 `..\用户手册.md`。）

---

## 一、主线：把副本做成"秘境化"（参考原神）

### 现在的样子

NWNDungeon 1.1.0 已经是**房间制关卡**：世界里随机生成带标记的铁门入口 → 红石激活 →
同区块玩家一起进独立副本世界 → 房间被铁门隔开、怪物提前摆好 → 清房给补给箱和开门拉杆 →
顶部 Boss 血条显示剩余怪物/首领血量 → 首领房清完给最终奖励 + 中央木门离开。

### 想去的方向

从"刷完就走"升级成原神秘境那种**有仪式感、有重复游玩动力**的循环：

选等级/难度 → 读条进本 → 限时清波次怪 → 结算评级 → 消耗体力领奖励

### 系统拆解（原神 → Minecraft 的对应做法与工程量）

| 原神的东西 | Minecraft 里的做法 | 工程量 |
| --- | --- | --- |
| 秘境入口 + 传送锚点 + 地图标记 | 保留现有随机门，额外加锚点与地图标记 | 小 |
| 读条进本 + 队伍 | 已有同区块组队，补 3 秒进度条与粒子 | 小 |
| **波次刷怪**（不是一次性摆满） | 改为按波生成，每波提示「第 2/4 波」 | 中 |
| 结算界面 | 通关弹 GUI：用时 / 击杀 / 评级 / 掉落 | 中 |
| S / A / B 评级 | 按用时与死亡次数算分 | 小 |
| 树脂（体力限制） | 按玩家记录每日副本次数 | 中 |
| 深渊（层数 + 每周重置 + 排行榜） | 多难度阶梯 + 每周重置 + 榜单 | 大 |
| **元素反应** | 用药水效果与自定义附魔模拟（火=点燃、冰=减速、雷=连锁） | 很大 |
| **圣遗物随机词条** | 自定义物品属性系统（PDC 存词条 + 属性计算） | 很大 |
| 4 人联机 | 现有机制加个上限即可 | 小 |

### 三步走

**第一步（一轮迭代就能看到效果）**

- 波次刷怪：每间房分 2~4 波，清完一波才出下一波，带提示
- 读条进本：3 秒进度条 + 音效 + 粒子，而不是瞬移
- 结算界面：通关弹出 GUI（用时、击杀数、死亡数、掉落）
- S / A / B 评级：按用时与死亡次数算

这四样做完，基本就有"秘境感"了。

**第二步**

- 每日次数限制（体力系统）
- 进本前选难度（现在只能靠门口方块区分）
- 独立掉落表与保底机制

**第三步（可选，大工程）**

- 元素系统、圣遗物词条
- 动手前先评估：是不是直接上 MMOItems / ItemsAdder 那一套更划算

### 已定方向

**怪物改为波次刷新**（2026-09-12 决定）。原来是一次性摆满，改为分波生成，体验更接近秘境。

### 波次系统的实现方案（下一步开工用）

**配置格式**：把房间的阵容从"一条字符串"改成"多波"，例如

```yaml
rooms:
  - waves:
      - "ZOMBIE:3"
      - "ZOMBIE:2,SKELETON:2"
```

兼容：仍支持单条字符串（视为只有一波）。

**运行逻辑**

1. 进本时只生成第 1 波，其余波次待命
2. 当前波清空 → 延迟 3 秒 → 提示「第 2/4 波」+ 音效 → 生成下一波
3. 最后一波清空 → 判定房间完成（给补给箱 + 门旁拉杆，首领房给最终奖励 + 木门）
4. Boss 房：首领放在最后一波，前面几波是小怪

**Boss 血条**：普通房显示「第 2 间 · 第 2/4 波 · 剩余 5 只」；
进度条按"本波剩余比例"走；首领波切换为「首领 45/60」。

**其它配套（同一步做完）**

- 读条进本：进副本前 3 秒进度条 + 音效 + 粒子，替换现在的瞬移
- 结算界面：通关后弹 GUI，显示用时、击杀数、死亡数、评级
- S / A / B 评级：按用时与死亡次数算分

### 待决策

**重启窗口**：改代码后需要一次服务器重启，安排在什么时间。

---

## 二、其它方向

**权限体系整理**：LuckPerms 现在只有 `default` / `owner` 两个组，管理员权限实际来自 OP。
建议加 `member` / `vip` / `helper`，设权重与聊天前缀。这是隐患项，优先级高。

**宣传推广**：计划书与投稿文案已完成（`nwn-promo\`）。第一梯队三件事：MC 百科找服玩收录、
MineBBS 发帖、QQ 群互宣。第二梯队做 B 站内容——**AI 幽灵是最好的题材**，中文圈少见。

**基岩版互通**：装 Geyser + Floodgate 让手机/基岩版玩家能进，代价是要再买一个 UDP 端口（100 积分）。

**服务器卫生**：清理 `plugins/_old-20260912/`、根目录旧核心 `Purpur-1.21.1-build2304.jar`、
`start.sh.bak-*` 等；确认面板备份策略（升级前那次备份可以留一段时间）。

**NWNDungeon 开源**：还没建仓库。McBot 已经开源了，副本插件也同样走一遍流程即可。

---

## 三、明确不做 / 暂缓

- **元素反应、圣遗物词条**：不是单个插件的量级，等于做一个 RPG 框架。除非决定上 MMOItems 那套，
  否则不做。
- **和现有经济体系冲突的重叠系统**：QuickShop（箱子商店）、SweetPlayerMarket（全球市场）已经在跑，
  新玩法不要再塞第二套交易体系。
- **脱离本服的通用化**：这套插件是"服务这一台服"的，不做成面向所有服的通用产品（那要考虑
  兼容性、GUI 编辑器、文档，成本完全不同）。

---

## 四、做新功能时的几条原则（踩坑总结）

- 先小步验证再扩：一个系统能跑通再加下一个（今天升级就是这样，先上核心再上插件）
- 涉及不兼容风险的操作必须留退路（备份、`_old-` 目录、旧配置 `.bak`）
- 换 jar 必须重启，改配置才能热重载——排期时要算进重启次数
- 面向玩家的机制尽量"原版化"（铁门、拉杆、方块表示难度），少用插件界面

## 附录 B：原 `交接.md` §6 — NWNDungeon 说明（2026-09-12）

## 6. 自研插件二：NWNDungeon（随机副本）

**入口**：世界里随机生成"副本门"——5×5 石砖平台 + **铁门** + 地面**石按钮** + 门底下的**难度方块**（铁块 / 金块 / 钻石块）+ 告示牌。

**生成方式**：监听新区块生成（`ChunkLoadEvent.isNewChunk`），概率 1/400（配置 `generation.chance`），只在地表合适方块上生成，所以只出现在没人探索过的地方。

**触发**：门旁**石按钮**通电（按一下约 1 秒）→ **门所在区块内的所有玩家一起进去**（单人就是单人，多人就是组队）。

**入口登记与失效**：铁门存不了 NBT，所以标记写在**区块的持久化数据**里（`Chunk#getPersistentDataContainer`）。门被破坏 → 标记为已损毁，**原地补新门也不会再触发**；触发时还会再校验一次门是否还在。

**副本内容**（v1.1.0 起是房间制关卡）：

- 怪物是**提前摆好的实体**（不是刷怪笼），全部持久化，不会自然消失
- 房间之间用**关闭的铁门**隔开（**门朝东、与走廊同轴**才挡得住人；旧版写死朝北，门板横卡在门框里、贴边能蹭过去）
- **把该房间的怪全打完**，门旁才生成**石按钮**、房间角落生成**补给箱**（最多 3 种、每种 1~2 个）；没打完时插件每秒强制保持门关闭、按钮不许出现
- 1.2.1 修的两处：**补给箱之前是空的**（方块刚放下就用快照背包填充、`update()` 反而把空背包盖回去 → 改成下一 tick 用实时容器写 + 回读校验 + 快照兜底）；**怪清完仍显示"剩余 N 只"**（旧版门不挡怪、怪会溜出房间 → 随朝向修复；另加每秒剔除已消失的怪）
- 1.2.2：**测试本打完所有房间自动释放槽位**（不再占着等重启；没打完则等到限时结束），并新增 `/dungeon release <槽位|all>` 手动回收
- 1.3.0（= `未来发展方向.md` 第一步）：**波次刷怪**（`rooms: - waves: [...]`，兼容旧单字符串；清完一波隔 3 秒刷下一波，带标题/音效；首领在最后一波随 `guards` 登场）、**读条进本**（3 秒进度条+粒子+音效，跑远/死亡/掉线取消，`trigger.cast-seconds` 可调 0 恢复瞬移）、**通关结算 GUI**（用时/击杀/死亡/最终奖励/评级）、**S/A/B 评级**（按用时与死亡次数）
- **踩坑（2026-09-18 发现）**：服务器上 `plugins/NWNDungeon/config.yml` 一直是**早期原型格式**（`rooms: 5` + `mobs: [...]` + `spawners-per-room`，不是现在插件读的多波列表），插件实际吃的是 jar 里打包的那份配置。已把旧文件备份为 `plugins/NWNDungeon/config.yml.bak-20260918`，并上传与仓库一致的 `config.yml`（4060 字节，含 waves / cast-seconds）。**以后改配置要同时改仓库和服务器上的那份**。
- 实测手法（可复用）：`dungeon test iron` + `execute in minecraft:nwndungeon run forceload add 0 0 240 32` → 用 `if entity @e[type=zombie,distance=..24]` 数每波怪（iron 第 1 间应为 3 → 2 只）、`minecraft:kill` 清波、`dungeon list` 看槽位是否自动回收；完事记得 `forceload remove all`。
- 屏幕上方 **Boss 血条**：普通房显示「第 N 间 · 剩余 M 只」，首领房显示「首领 45/60」
- 首领房清完 → **最终奖励箱** + 房间中央生成**木门**，右键离开副本
- 阵容按难度递进（近战顶前排 + 远程压制 + 蜘蛛/苦力怕逼走位），配置在 `config.yml` 的 `tiers.*.rooms`，格式 `"ZOMBIE:5,SKELETON:3"`

**副本内规则**：冒险模式（能拉机关、不能破坏/放置地形）、爆炸全部拦截、死亡不掉落并回到最近检查点、限时 20/15/10 分钟（按难度）。

**指令**：`/dungeon spawn [iron|gold|diamond]`（在玩家面前 3 格生成入口）、`leave`、`list`、`tp <槽位>`、`reload`、`test <难度>`（管理员测试生成，不占玩家）

**已实测（2026-09-18 夜，RCON 直测）**：按钮触发、铁门朝向与开合、清完才出下一间按钮、补给箱与最终奖励箱发物品、中央木门出现。
**仍未实测**：门被破坏后的永久失效、自然随机生成（需要玩家/新区块配合）；Boss 血条是图形界面，建议玩家肉眼确认一次。

---
