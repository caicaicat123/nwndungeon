# 更新记录

## 1.4.0

- **入口改成"遗迹"建筑**，规模按难度递进：铁 7×7 / 金 9×9 / 钻 11×11（墙高、装饰与残缺程度一起变多）；
  风格是残缺石砖 + 苔石 + 断柱 + 灯笼（四角与门洞两侧立柱挂灯笼），地面自动垫平 / 削平，不会悬空
- **门上方那格也换成对应难度的方块**（铁块 / 金块 / 钻石块），与门下面那块呼应；告示牌立在门楣上
- **入口恢复成石按钮**（凿制石砖台座，西侧台座放灯笼对称）：按一下才进本，避免路过误触发；告示牌文案回到「按下按钮进入」
- 房间之间的开门仍用石压力板（清完一间才出现）

## 1.3.1

- 入口与房间开门的**石按钮改成石压力板**：
  - 入口的板子铺在门前地板上（玩家走近门自然踩到），踩上去给门通电 → 触发进本读条
  - 房间的板子铺在门前走廊地板上，踩上去给铁门通电 → 门开；告示牌文案同步改成「踩下压力板进入」

## 1.3.0

- **波次刷怪**：房间阵容支持多波（`rooms: - waves: [...]`，兼容旧的单字符串写法）。
  进本只刷第一波，清完一波隔 3 秒刷下一波，带标题与音效提示「第 2/4 波」；
  首领房的 `boss.waves` 是前面的小怪波，最后一波 = `guards` + 首领本人
- **读条进本**：按下入口按钮后亮起 3 秒进度条（粒子 + 音效），读条期间跑远（`trigger.cancel-distance`，默认 6 格）、
  死亡或掉线会取消本次进入；`trigger.cast-seconds: 0` 可关掉读条恢复瞬移
- **通关结算界面**：首领房清完弹出只读 GUI，显示用时 / 击杀 / 死亡 / 最终奖励 / 评级
- **S/A/B 评级**：用时 ≤ 限时一半且零死亡 = S；≤ 3/4 限时且最多死一次 = A；其余 B
- Boss 血条改成「第 N 间 · 第 x/y 波 · 剩余 M 只」，首领登场后切成「首领 血量/上限」

## 1.2.2

- 测试本（`/dungeon test`）**打完所有房间后自动释放槽位**，不再占着等重启（没打完则等到限时结束）
- 新增 `/dungeon release <槽位|all>`：管理员随时手动回收槽位
- 补上 `/dungeon` 帮助与 Tab 补全里漏掉的 `test`

## 1.2.1

- 修正**补给箱是空的**：方块刚放下那一瞬间容器还没稳定，快照背包填完再 `update()` 会把空背包盖回去
  → 改成下一 tick 用实时容器写 + 回读校验 + 快照兜底
- 修正**怪清完仍显示「剩余 N 只」**：旧版铁门朝向错误导致怪能溜出房间
  → 随朝向修复；另加每秒兜底，名单里已消失的怪自动剔除

## 1.2.0

- 入口与房间的拉杆全部换成**石按钮**（门旁凿制石砖台座），告示牌文案同步改成「按下按钮进入」
- 修正房间铁门朝向：走廊是东西向的，门必须朝东/朝西，旧版写死朝北会让门板横卡在门框里、贴边能蹭过去
- 新增兜底：房间没清完时，通往下一间的铁门每秒强制保持关闭、按钮不许出现

## 1.1.0

- 副本改成**房间制关卡**：房间之间用关闭的铁门隔开，清完一间才给按钮 / 补给箱
- 新增检查点（磁石 + 告示牌）、Boss 血条、首领房最终奖励箱与中央离开木门
- 入口改成"提前摆好怪"而不是刷怪笼；怪物持久化，不会自然消失

## 更早（原型）

- 随机入口（5×5 石砖平台 + 铁门 + 难度方块 + 告示牌）与虚空副本世界、槽位隔离
