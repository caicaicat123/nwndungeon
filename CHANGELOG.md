# 更新记录

## 1.4.9

- **PlaceholderAPI 接口**（装了 PAPI 才注册，`plugin.yml` 里 `softdepend`）：
  `%nwndungeon_stamina%` 当前体力、`%nwndungeon_stamina_max%` 上限、`%nwndungeon_stamina_next%` 下一点秒数、
  `%nwndungeon_stamina_next_text%` 好看版（已满 / 3 分 20 秒）、`%nwndungeon_stamina_bar%` 进度条、
  `%nwndungeon_stamina_full%`；另有 `%nwndungeon_cost_<难度>%`（进本消耗）与 `%nwndungeon_money_<难度>%`（通关金币）
- DeluxeMenus 主菜单「玩家信息」里加了三行体力（当前/上限 + 进度条、下一点回复、各难度进本消耗）

## 1.4.8

- **掉落表拆成独立文件 `plugins/NWNDungeon/loot.yml`**：按难度分栏写补给箱 / 最终奖励箱
  （`{ item, weight, min, max, chance }`，也支持只写物品名）；`config.yml` 里不再放掉落表
- **确认面板拆成独立文件 `plugins/NWNDungeon/party.yml`**：`confirm-panel` 开关 +
  面板标题、说明文字、确认/取消按钮文字（支持 `&` 颜色代码与 `%count%` / `%tier%` 占位符）
- 两个文件改完执行 `/dungeon reload` 即可生效；文件不存在时会自动生成带注释的默认版本

## 1.4.7

- **掉落池随机化**：`supply-chest` / `reward-chest` 支持带参数的掉落表
  `{ item: BREAD, weight: 3, min: 1, max: 4, chance: 0.5 }`（weight 权重、min/max 数量、chance 每次是否进候选池）；
  纯物品名的旧写法照样兼容；三个难度的池子已按新格式重排（越难越肥）
- **进本确认面板**：入口区块里**不止一个人**时，按按钮不再直接开始读条，先弹 GUI 面板
  （列出这次会带谁进本 + 难度 + 「确认进入 / 取消」）；确认后才读条；多人路径不再因"离门远"掉队
  （单人路径保持原样）。开关：`trigger.confirm-panel`

## 1.4.6

- **通关发钱**：通关时给队伍里每个在线玩家发钱（走服务器经济 `eco give`）：
  普通 **1000** / 困难 **5000** / 噩梦 **20000**（配置 `tiers.<难度>.money-reward`，0 = 不发）；结算界面显示金额
- **体力（每日次数）**：默认 **500 点池、24 小时回满**（每 3456 tick = 172.8 秒回 1 点，离线照回，上线按时间差补算）
  - 进本消耗：普通 **25**（20 次）/ 困难 **33**（15 次）/ 噩梦 **50**（10 次），配置 `tiers.<难度>.stamina-cost`
  - 体力不够会被拦下（提示谁不够、各自还剩多少），读条结束真正进本时才扣；新增 `/dungeon stamina` 查询
  - 体力与"上次结算时间"写在玩家数据里，服务器重启不丢

## 1.4.5

- **修正"退出副本后还留在冒险模式"**：进本前会把原来的游戏模式**记进玩家数据**（重启也不丢），
  离开副本（`/dungeon leave` / 中央木门 / 通关压力板）、副本回收、掉线太久被请出去、重新上线时都会还原
- 兜底：重新上线时如果身上还留着"进本前的模式"记录、但人已经不在副本里，自动切回去；
  `/dungeon leave` 在"人不在副本里"时也会尝试还原，方便自救

## 1.4.4

- **掉线后可接续**：以前一掉线就被踢出这一局（配置里的 `instance.empty-recycle-minutes` 从没被读过）。
  现在掉线会**保留位置**，在宽限期（默认 5 分钟，配置项 `instance.empty-recycle-minutes`）内重新上线，
  会直接送回**最近的检查点**、恢复冒险模式、重新挂上 Boss 血条
- 离线超过宽限期、或这一局已经结束/回收：上线时按"回家票"送回**进本前的位置与原游戏模式**
  （以前这种情况人会被留在副本世界里出不来）
- 队伍里有人掉线时，副本不会再因为他掉线而立刻回收；全员离开或超时才会回收

## 1.4.3

- **房间内部结构重做**（开阔 + 精美）：
  - 净高 6 → **10 格**（大厅/战斗房/首领房都变成高顶石厅；走廊仍是低拱，形成"低走廊 → 高大房间"的对比）
  - 地板做成拼花：最外圈凿制石砖包边、内圈磨制安山岩走道、中间两条对角石带 + 石砖/裂缝/苔藓随机拼色
  - 墙面分三段：下段墙裙（磨制安山岩）、中段石砖、顶部两格檐口（凿制石砖）；**壁柱**向房间内凸出一格，从墙裙直通天花
  - 天花贴墙一圈半砖收口，四角上方**铁链吊灯**（链 2 格 + 悬挂灯笼）
  - 检查点平台加凿制石砖外框 + 头顶吊灯，告示牌前移一格不挡路
  - 走廊：每 6 格一道**拱肋**（凿制石砖），中央地板换成平滑石带，拱顶嵌海晶灯

## 1.4.2

- **房间编号改成"从第一间有怪的房间算起"**：入口大厅不再占号（它的检查点告示牌改标「入口大厅」），
  第一间有怪的房间 = 第 1 间；Boss 血条、清场广播、波次标题、检查点告示牌全部跟着改
- 1.5.0 的副本编辑器**先关着**（`EDITOR_ENABLED = false`，命令只提示"开发中"），其余功能不受影响

## 1.5.0（本地开发中，尚未部署）

- **开发者选项 / 副本编辑器**：`/dungeon edit new <名字>` 进独立的编辑世界（`nwndungeon_edit`，创造模式、一块 256×256 空地），
  手搭副本 → 用命令打标记 → `/dungeon edit save` 存成模板
  - 标记：`pos1` / `pos2`（选区）、`spawn`（进本落点）、`checkpoint`（检查点）、`chest`（看箱子标记奖励箱位）、
    `exit`（通关后出现的离开压力板）、`room <n>` / `wave <n>`（当前房间 / 波次）、`mob <模板>`（当前怪物）、
    `spawnpoint`（在脚下加一个刷怪点）、`gate`（看铁门登记为这一间的门）
  - `/dungeon edit info` 看已记内容，`/dungeon edit test <名字>` 立刻贴一份自己进去跑，`list` / `delete` 管理模板
  - 结构用 **Paper 自带的结构 API** 存成 `templates/<名字>.nbt`（方块 + 实体，不需要 WorldEdit），
    参数与标记存 `templates/<名字>.yml`（坐标相对选区原点）
  - 再次 `new` 同一个名字会把已保存的结构贴回工作区继续改
- **自定义怪物**：`mobs.yml` 定义（种类 / 名字 / **血量** / **攻击力** / 移速 / **装备 + 附魔** / 掉不掉装备 / 额外掉落），
  刷怪点用 `mob: <模板名>` 引用；自带 5 个示例（僵尸兵 / 精英僵尸 / 骷髅射手 / 沙丘蛮兵 / 亡灵法师 BOSS）。
  不依赖 MythicMobs（服务器不装也能跑）
- **模板副本运行时**：`/dungeon template <名字>` 开一把 —— 复用现有的波次推进、Boss 血条、结算 GUI、评级、检查点逻辑；
  打完最后一间后通关压力板出现在标记位置，踩上去离开副本
- 与现有随机副本**并存**：程序生成的随机副本、随机入口生成全部保持原样

## 1.4.1

- 进本时**检查点就在脚下**：大厅的磁石检查点平台直接铺在出生点正下方（其余检查点仍在偶数房间西侧，避开 9 个刷怪点）
- 进本提示补一句「脚下的磁石平台就是第一个检查点」，并拆分离开方式提示

## 1.4.0

- **入口改成"遗迹"建筑**，规模按难度递进：铁 7×7 / 金 9×9 / 钻 11×11（墙高、装饰与残缺程度一起变多）；
  风格是残缺石砖 + 苔石 + 断柱 + 灯笼（四角与门洞两侧立柱挂灯笼），地面自动垫平 / 削平，不会悬空
- **门上方那格也换成对应难度的方块**（铁块 / 金块 / 钻石块），与门下面那块呼应；告示牌立在门楣上
- **入口恢复成石按钮**（凿制石砖台座，西侧台座放灯笼对称）：按一下才进本，避免路过误触发；告示牌文案回到「按下按钮进入」
- 房间之间的开门仍用石压力板（清完一间才出现）

## 1.3.1

- 入口与房间开门的**石按钮改成石压力板**：
  - 入口的板子铺在门前地板上（玩家走近门自然踩到），踩上去给门通电 → 触发进本读条
  - 房间的板子铺在门前走廊地板上，踩上去给铁门通电 → 门开；告示牌文案同步改成「踩下压力板进入」

## 1.3.0

- **波次刷怪**：房间阵容支持多波（`rooms: - waves: [...]`，兼容旧的单字符串写法）。
  进本只刷第一波，清完一波隔 3 秒刷下一波，带标题与音效提示「第 2/4 波」；
  首领房的 `boss.waves` 是前面的小怪波，最后一波 = `guards` + 首领本人
- **读条进本**：按下入口按钮后亮起 3 秒进度条（粒子 + 音效），读条期间跑远（`trigger.cancel-distance`，默认 6 格）、
  死亡或掉线会取消本次进入；`trigger.cast-seconds: 0` 可关掉读条恢复瞬移
- **通关结算界面**：首领房清完弹出只读 GUI，显示用时 / 击杀 / 死亡 / 最终奖励 / 评级
- **S/A/B 评级**：用时 ≤ 限时一半且零死亡 = S；≤ 3/4 限时且最多死一次 = A；其余 B
- Boss 血条改成「第 N 间 · 第 x/y 波 · 剩余 M 只」，首领登场后切成「首领 血量/上限」

## 1.2.2

- 测试本（`/dungeon test`）**打完所有房间后自动释放槽位**，不再占着等重启（没打完则等到限时结束）
- 新增 `/dungeon release <槽位|all>`：管理员随时手动回收槽位
- 补上 `/dungeon` 帮助与 Tab 补全里漏掉的 `test`

## 1.2.1

- 修正**补给箱是空的**：方块刚放下那一瞬间容器还没稳定，快照背包填完再 `update()` 会把空背包盖回去
  → 改成下一 tick 用实时容器写 + 回读校验 + 快照兜底
- 修正**怪清完仍显示「剩余 N 只」**：旧版铁门朝向错误导致怪能溜出房间
  → 随朝向修复；另加每秒兜底，名单里已消失的怪自动剔除

## 1.2.0

- 入口与房间的拉杆全部换成**石按钮**（门旁凿制石砖台座），告示牌文案同步改成「按下按钮进入」
- 修正房间铁门朝向：走廊是东西向的，门必须朝东/朝西，旧版写死朝北会让门板横卡在门框里、贴边能蹭过去
- 新增兜底：房间没清完时，通往下一间的铁门每秒强制保持关闭、按钮不许出现

## 1.1.0

- 副本改成**房间制关卡**：房间之间用关闭的铁门隔开，清完一间才给按钮 / 补给箱
- 新增检查点（磁石 + 告示牌）、Boss 血条、首领房最终奖励箱与中央离开木门
- 入口改成"提前摆好怪"而不是刷怪笼；怪物持久化，不会自然消失

## 更早（原型）

- 随机入口（5×5 石砖平台 + 铁门 + 难度方块 + 告示牌）与虚空副本世界、槽位隔离
